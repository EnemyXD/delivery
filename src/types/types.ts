export type AdvertisementType = {
  createdAt: string;
  updatedAt: string;
  description: string;
  images: string[];
  isDeleted: boolean;
  shortText: string;
  tags: string[];
  userId: string;
};
